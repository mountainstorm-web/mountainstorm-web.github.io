---
label: just-fields
hide_body: true
fields:
- name: dew
  type: field_group_list
  fields:
  - name: yy
    type: text
    config:
      required: false
    label: yy
  config:
    min: 
    max: 
    labelField: 
  label: dew
pages:
- _posts/test.md
